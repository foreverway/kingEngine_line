export default interface IDict {
  // 字典值
  dictValue: string
  // 字典名称
  dictName: string
  // 字典code
  dictCode: string
  // 层级
  dictLevel: number
  // 排序
  orderNo: number
  // 备注
  note: string
}
